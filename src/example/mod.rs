use super::*;

::modwire::expose!(
    // flatten any nested modules for consumption
    
);

pub struct Plugin;

impl super::Plugin for Plugin {
    fn build(&self, app: &mut App) {
        
    }
}

pub fn spawn(mut commands: Commands) {

}